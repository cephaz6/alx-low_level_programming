#include <stdio.h>


/**
 * main - The conventional entry point of a c program
 * Return: When run completes, 0 is expected to be returned
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return(0);
}
